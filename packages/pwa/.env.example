## PWA (Vite) environment variables
#
# Copy to: packages/pwa/.env
#
# API base URL - uses relative path by default for nginx proxy
# In production: "/api" (proxied by nginx to hailmary-api)
# In development: Set to "http://localhost:3001" if not using nginx proxy
VITE_API_BASE=/api

# Admin Agent base URL - uses relative path by default for nginx proxy
# In production: "/admin-agent" (proxied by nginx to hailmary-admin-agent)
# In development: Set to "http://localhost:4010" if not using nginx proxy
VITE_ADMIN_AGENT_BASE=/admin-agent

