/**
 * Rocky App - Desktop Window Wrapper
 * 
 * Wraps the RockyTool component for use in the desktop window system.
 */

import React from 'react';
import { RockyTool } from '../../../modules/rocky';

export const RockyApp: React.FC = () => {
  return <RockyTool />;
};
