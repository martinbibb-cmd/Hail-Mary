/**
 * Sarah App - Desktop Window Wrapper
 * 
 * Wraps the SarahTool component for use in the desktop window system.
 */

import React from 'react';
import { SarahTool } from '../../../modules/sarah';

export const SarahApp: React.FC = () => {
  return <SarahTool />;
};
