{
  "name": "@hail-mary/shared",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc && npm run copy-json",
    "copy-json": "mkdir -p dist/core && cp src/core/*.json dist/core/",
    "dev": "tsc --watch",
    "lint": "eslint src --ext .ts"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
